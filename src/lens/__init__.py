"""lens — statistics over powdr APC JSON dumps.

A git-like CLI (``show`` / ``compare``) that resolves dump filenames from
``(group, block, step)`` and reports structural statistics, with human
(``rich``), ``--plain`` and ``--json`` output modes.
"""
